[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pokemon-card-bot"
version = "0.1.0"
description = "Safe, retailer-agnostic Pokemon card inventory monitor"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["pokemon_bot*"]
